---
dependency:
  name: galaxy

driver:
  name: podman

platforms:
  - name: ubuntu
    image: docker.io/geerlingguy/docker-ubuntu2204-ansible
    pre_build_image: true

provisioner:
  name: ansible

verifier:
  name: ansible